# 📁 项目结构详解

完整的项目目录结构和各文件的说明。

## 项目总体结构

```
ai-role-play/
├── 📄 README.md                    # 项目主说明文档 ⭐ 从这里开始
├── 📄 GETTING_STARTED.md           # 快速入门指南
├── 📄 CONTRIBUTING.md              # 贡献指南
├── 📄 PROJECT_STRUCTURE.md         # 项目结构说明（你在这里）
├── 📄 .env.example                 # 环境变量模板
├── 📄 .gitignore                   # Git 忽略文件
├── 📄 LICENSE                      # MIT 许可证
│
├── 📁 server-go/                   # Go 后端服务 🚀
│   ├── main.go                     # 程序入口
│   ├── roleplay.api                # API 定义文件（go-zero）
│   ├── go.mod                      # Go 模块定义
│   ├── go.sum                      # Go 依赖校验和
│   ├── README.md                   # Go 服务说明
│   ├── etc/                        # 配置文件目录
│   │   └── roleplay-api.yaml       # 服务配置文件
│   └── internal/                   # 内部代码（不对外暴露）
│       ├── config/                 # 配置定义
│       │   └── config.go           # 配置结构体
│       ├── handler/                # HTTP 请求处理器
│       │   ├── routes.go           # 路由注册
│       │   ├── getroleshandler.go  # 获取角色列表
│       │   ├── getrolehandler.go   # 获取角色详情
│       │   ├── sendmessagehandler.go # 发送消息
│       │   ├── gethistoryhandler.go # 获取历史
│       │   └── setroleapihandler.go # 设置角色
│       ├── logic/                  # 业务逻辑层
│       │   ├── getroleslogic.go    # 获取角色逻辑
│       │   ├── getrolelogic.go     # 获取角色详情逻辑
│       │   ├── sendmessagelogic.go # 发送消息逻辑
│       │   ├── gethistorylogic.go  # 获取历史逻辑
│       │   └── setroleapilogic.go  # 设置角色逻辑
│       ├── svc/                    # 服务上下文（依赖注入）
│       │   └── servicecontext.go   # 服务上下文定义
│       ├── types/                  # 数据类型定义
│       │   └── types.go            # 所有请求/响应类型
│       └── grpc/                   # gRPC 客户端
│           ├── client.go           # gRPC 客户端实现
│           ├── role_play.pb.go     # Protocol Buffer 定义
│           └── role_play_grpc.pb.go # gRPC 生成代码
│
├── 📁 server-python/               # Python AI 服务 🐍
│   ├── role_play_cli.py            # CLI 应用入口
│   ├── requirements.txt            # Python 依赖列表
│   ├── .env.example                # 环境变量模板
│   ├── README.md                   # Python 服务说明
│   └── grpc/                       # gRPC 服务实现
│       ├── server.py               # gRPC 服务器主程序
│       ├── role_play.proto         # Protocol Buffer 定义
│       ├── role_play_pb2.py        # Protocol Buffer 生成代码
│       ├── role_play_pb2_grpc.py   # gRPC 生成代码
│       ├── role_play_grpc_client.py # gRPC 客户端（测试用）
│       └── test_grpc_connection.py # 连接测试脚本
│
├── 📁 role-play-vue/               # Vue 3 Web 前端 🎨
│   ├── index.html                  # HTML 入口文件
│   ├── package.json                # NPM 依赖配置
│   ├── vite.config.js              # Vite 构建配置
│   ├── README.md                   # Vue 前端说明
│   └── src/                        # 源代码目录
│       ├── main.js                 # Vue 应用入口
│       ├── App.vue                 # 根组件
│       ├── assets/                 # 静态资源
│       │   └── logo.svg            # 项目 Logo
│       ├── components/             # 可复用组件
│       │   ├── Navbar.vue          # 导航栏组件
│       │   ├── MessageBubble.vue   # 消息气泡组件
│       │   └── ChatInput.vue       # 聊天输入组件
│       ├── views/                  # 页面组件
│       │   ├── HomeView.vue        # 首页
│       │   ├── RoleListView.vue    # 角色列表页
│       │   └── ChatView.vue        # 聊天页面
│       ├── router/                 # 路由配置
│       │   └── index.js            # 路由定义
│       ├── stores/                 # 状态管理（Pinia）
│       │   └── roleStore.js        # 角色和聊天状态
│       ├── services/               # API 服务层
│       │   ├── api.js              # Axios 实例配置
│       │   └── roleService.js      # 角色和聊天 API
│       └── styles/                 # 全局样式
│           └── global.css          # 全局 CSS
│
├── 📁 app/                         # Android APP（Kivy）📱
│   ├── main.py                     # APP 主程序
│   ├── buildozer.spec              # Android 打包配置
│   ├── requirements.txt            # Python 依赖
│   ├── .env.example                # 环境变量模板
│   └── README.md                   # APP 说明
│
└── 📁 ai_role_play_app/            # 其他资源
    └── ...                         # 其他文件
```

---

## 各模块详细说明

### 🚀 server-go（Go 后端服务）

**职责**：REST API 网关，处理来自前端的 HTTP 请求

**关键文件**：

| 文件 | 说明 |
|------|------|
| `main.go` | 程序入口，初始化服务 |
| `roleplay.api` | API 定义（go-zero 框架） |
| `internal/handler/` | HTTP 请求处理器 |
| `internal/logic/` | 业务逻辑实现 |
| `internal/svc/` | 服务上下文（依赖注入） |
| `internal/types/` | 数据类型定义 |
| `etc/roleplay-api.yaml` | 服务配置 |

**工作流程**：
```
HTTP 请求 → Handler（参数验证）→ Logic（业务逻辑）→ gRPC 调用 → 返回响应
```

**API 接口**：
- `GET /roles` - 获取角色列表
- `GET /roles/:id` - 获取角色详情
- `POST /messages` - 发送消息
- `GET /messages/:role_id` - 获取对话历史

---

### 🐍 server-python（Python AI 服务）

**职责**：AI 模型调用，处理复杂业务逻辑

**关键文件**：

| 文件 | 说明 |
|------|------|
| `grpc/server.py` | gRPC 服务器主程序 |
| `role_play_cli.py` | CLI 应用和角色定义 |
| `grpc/role_play.proto` | Protocol Buffer 定义 |
| `requirements.txt` | Python 依赖 |

**支持的 LLM**：
- OpenAI (GPT-4, GPT-3.5)
- 智谱 AI (GLM-4)
- 硅基流动 (多个开源模型)

**工作流程**：
```
gRPC 请求 → 获取角色信息 → 调用 AI 模型 → 返回响应
```

---

### 🎨 role-play-vue（Vue 3 Web 前端）

**职责**：用户界面，提供聊天交互

**关键文件**：

| 文件 | 说明 |
|------|------|
| `src/main.js` | Vue 应用入口 |
| `src/App.vue` | 根组件 |
| `src/views/` | 页面组件 |
| `src/components/` | 可复用组件 |
| `src/router/` | 路由配置 |
| `src/stores/` | 状态管理 |
| `src/services/` | API 服务 |

**页面结构**：
```
首页 (HomeView)
  ↓ 点击"进入"
角色列表 (RoleListView)
  ↓ 选择角色
聊天页面 (ChatView)
  ↓ 发送消息
AI 回复
```

**技术栈**：
- Vue 3 (Composition API)
- Vite (构建工具)
- Pinia (状态管理)
- Vue Router (路由)
- Axios (HTTP 客户端)

---

### 📱 app（Android APP）

**职责**：移动端应用

**关键文件**：

| 文件 | 说明 |
|------|------|
| `main.py` | APP 主程序 |
| `buildozer.spec` | Android 打包配置 |
| `requirements.txt` | 依赖 |

**技术栈**：
- Kivy (跨平台 UI 框架)
- Python 3.9+

---

## 数据流向

### 用户与 AI 对话的完整流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户在 Web 前端输入消息                                   │
│    (role-play-vue/ChatView.vue)                              │
└────────────────────┬────────────────────────────────────────┘
                     │ HTTP POST /messages
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Go 服务接收请求                                           │
│    (server-go/handler/sendmessagehandler.go)                │
└────────────────────┬────────────────────────────────────────┘
                     │ 参数验证
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. Go 服务执行业务逻辑                                       │
│    (server-go/logic/sendmessagelogic.go)                    │
└────────────────────┬────────────────────────────────────────┘
                     │ gRPC 调用
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Python 服务处理请求                                       │
│    (server-python/grpc/server.py)                           │
└────────────────────┬────────────────────────────────────────┘
                     │ 调用 AI 模型
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. AI 模型生成回复                                           │
│    (OpenAI / 智谱 AI / 硅基流动)                            │
└────────────────────┬────────────────────────────────────────┘
                     │ 返回响应
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. Python 服务返回 gRPC 响应                                 │
└────────────────────┬────────────────────────────────────────┘
                     │ gRPC 响应
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. Go 服务返回 HTTP 响应                                     │
└────────────────────┬────────────────────────────────────────┘
                     │ JSON 响应
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 8. Web 前端显示 AI 回复                                      │
│    (role-play-vue/ChatView.vue)                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 文件命名规范

### Go 文件

```
handler/
  ├── getroleshandler.go        # 小写，驼峰式
  ├── sendmessagehandler.go
  └── routes.go

logic/
  ├── getroleslogic.go
  ├── sendmessagelogic.go
  └── ...
```

### Python 文件

```
grpc/
  ├── server.py                 # 小写，下划线分隔
  ├── role_play.proto
  ├── role_play_pb2.py
  └── test_grpc_connection.py
```

### Vue 文件

```
components/
  ├── MessageBubble.vue         # PascalCase
  ├── ChatInput.vue
  └── Navbar.vue

views/
  ├── HomeView.vue
  ├── RoleListView.vue
  └── ChatView.vue
```

---

## 配置文件说明

### .env 文件

```env
# API Keys
OPENAI_API_KEY=sk-xxx...
ZHIPU_API_KEY=xxx...

# 服务配置
GO_PORT=8080
GRPC_PORT=50051

# 数据库
DB_HOST=localhost
DB_PORT=3306
```

### roleplay-api.yaml (Go 配置)

```yaml
Name: ai-role-play-app-go-server
Host: 0.0.0.0
Port: 8080
Timeout: 30000

DataSource:
  Host: localhost
  Port: 3306
  Database: roleplay
  Username: root
  Password: password

GrpcService:
  Address: localhost:50051
  Timeout: 30000
```

### vite.config.js (Vue 配置)

```javascript
export default {
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  }
}
```

---

## 依赖关系

### Go 依赖

```
go-zero (框架)
├── rest (REST 服务)
├── grpc (gRPC 客户端)
└── logx (日志)

google.golang.org/grpc (gRPC)
google.golang.org/protobuf (Protocol Buffer)
```

### Python 依赖

```
grpcio (gRPC)
grpcio-tools (Protocol Buffer 编译)
openai (OpenAI API)
zhipuai (智谱 AI API)
python-dotenv (环境变量)
```

### Vue 依赖

```
vue (框架)
vite (构建工具)
pinia (状态管理)
vue-router (路由)
axios (HTTP 客户端)
```

---

## 开发工作流

### 添加新功能的步骤

1. **定义 API**（Go）
   - 编辑 `server-go/roleplay.api`
   - 运行 `goctl api go -api roleplay.api -dir .`

2. **实现业务逻辑**（Go）
   - 编辑 `server-go/internal/logic/` 中的文件

3. **实现 AI 处理**（Python）
   - 编辑 `server-python/grpc/server.py`

4. **更新前端**（Vue）
   - 编辑 `role-play-vue/src/services/`
   - 编辑 `role-play-vue/src/views/` 或 `components/`

5. **测试**
   - 测试 API
   - 测试前端交互
   - 测试 AI 响应

---

## 常用命令

```bash
# Go 服务
cd server-go
go mod tidy              # 下载依赖
go run main.go           # 运行服务
goctl api go -api roleplay.api -dir .  # 生成代码

# Python 服务
cd server-python
pip install -r requirements.txt  # 安装依赖
python grpc/server.py    # 运行服务
python grpc/test_grpc_connection.py  # 测试连接

# Vue 前端
cd role-play-vue
npm install              # 安装依赖
npm run dev              # 开发模式
npm run build            # 生产构建
npm run preview          # 预览构建
```

---

## 下一步

- 📖 阅读 [快速入门指南](./GETTING_STARTED.md)
- 🚀 查看 [项目 README](./README.md)
- 🤝 了解 [贡献指南](./CONTRIBUTING.md)
- 📚 查看各模块的详细文档

---

希望这个结构说明能帮助你理解项目！如有疑问，欢迎提出 Issue。🎉
